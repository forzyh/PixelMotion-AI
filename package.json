{
  "name": "pixelmotion-ai",
  "version": "1.0.0",
  "description": "AI-powered pixel sprite motion sheet generator",
  "main": "dist-electron/main.js",
  "scripts": {
    "dev": "npm run build:main && npm run build:preload && npx vite --config electron.vite.config.ts",
    "build": "npm run build:main && npm run build:preload && npx vite build --config electron.vite.config.ts",
    "build:main": "node -e \"require('fs').mkdirSync('dist-electron', {recursive: true})\" && npx esbuild electron/main.ts --bundle --platform=node --target=node18 --outfile=dist-electron/main.js --format=cjs --external:sharp --external:electron-store --external:openai --external:gifenc --external:electron",
    "build:preload": "npx esbuild electron/preload.ts --bundle --platform=node --target=node18 --outfile=dist-electron/preload.js --format=cjs --external:electron",
    "preview": "npm run build && npx vite preview",
    "dist:win": "electron-builder --win --config electron-builder.config.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "electron-store": "^8.1.0",
    "gifenc": "^1.0.3",
    "openai": "^4.28.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "sharp": "^0.33.2"
  },
  "devDependencies": {
    "@types/node": "^20.11.16",
    "@types/react": "^18.2.55",
    "@types/react-dom": "^18.2.19",
    "@vitejs/plugin-react": "^4.2.1",
    "autoprefixer": "^10.4.17",
    "electron": "^29.4.6",
    "electron-builder": "^24.9.1",
    "electron-rebuild": "^3.2.9",
    "electron-vite": "^2.0.0",
    "esbuild": "^0.20.2",
    "postcss": "^8.4.35",
    "tailwindcss": "^3.4.1",
    "typescript": "^5.3.3",
    "vite": "^5.1.0"
  }
}
